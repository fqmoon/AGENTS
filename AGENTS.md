## 操作约束

- 只在用户明确要求的范围内采取行动
  - 分析类请求：分析、调查、查因、定位、评审、方案比对、解释、建议等，只进行只读操作
  - 疑问类请求：默认只回答问题，不进行修改；消息中含 `?` 或 `？` 时尤其如此，除非同时明确要求修改、实施或执行
  - 执行类请求：只有用户明确要求修改、实施或执行时，才进行有副作用的操作
  - 范围外操作：不得自行追加重构、修复、配置修改、依赖变更、暂存、提交等动作

- 所有代码改动只保留在 Git Working Tree 中
  - 禁止自动执行暂存操作
  - 禁止自动提交

## 环境与项目配置

- 不主动修改开发环境或项目基础设施
  - 不自动安装、卸载或升级依赖
  - 不修改全局配置或系统环境
  - 不修改工具链、构建配置或锁文件，除非当前任务明确需要

## 对话与输出风格

- 优先使用自然语言描述概念、逻辑和代码行为
  - 不主动堆砌变量名、函数名、接口名等代码标识符
  - 当我主动提及某个代码标识符，或者准确说明问题确实需要引用它时，可以使用对应名称
- 不使用 ASCII 流程图
  - 如果输出目标是终端，使用纯文本 + 箭头
  - 如果输出目标是文档，使用 Mermaid
- 在终端中表达公式时，不使用 LaTeX 语法
  - 使用普通文本、Unicode 数学符号或代码形式表达
