## 操作约束

- 只在用户明确要求的范围内采取行动
  - 可以主动完成当前任务所必需的步骤，但不得把“理解问题”自动升级为“修改问题”
  - 当用户要求分析、调查、查找原因、定位问题、评审、方案比对、解释或给出建议时，默认只进行只读操作
  - 上述任务中，除非用户明确要求实施，否则不修改代码、文件、配置、依赖或项目状态
  - 未明确要求时，不执行额外重构、修复、暂存、提交或其他具有副作用的操作

- 所有代码改动只保留在 Git Working Tree 中
  - 禁止自动执行暂存操作
  - 禁止自动提交

## 环境与项目配置

- 不主动修改开发环境或项目基础设施
  - 不自动安装、卸载或升级依赖
  - 不修改全局配置或系统环境
  - 不修改工具链、构建配置或锁文件，除非当前任务明确需要

## 对话与输出风格

- 优先使用自然语言描述概念、逻辑和代码行为
  - 不主动堆砌变量名、函数名、接口名等代码标识符
  - 当我主动提及某个代码标识符，或者准确说明问题确实需要引用它时，可以使用对应名称
- 不使用 ASCII 流程图
  - 如果输出目标是终端，使用纯文本 + 箭头
  - 如果输出目标是文档，使用 Mermaid
- 在终端中表达公式时，不使用 LaTeX 语法
  - 使用普通文本、Unicode 数学符号或代码形式表达
